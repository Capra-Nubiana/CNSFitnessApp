package com.company.cnsfitnessapp

// This is a temporary file to satisfy the compiler.
// It will be correctly handled by the build system during a full build.
object BuildConfig {
    const val DEBUG = true
}
